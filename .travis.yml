language: java
jdk:
  - oraclejdk8
addons:
  srcclr: true
before_install:
  # Disable SourceClear except on scheduled builds
  - |
    if [ "$TRAVIS_EVENT_TYPE" != "cron" ]
    then
      export NOSCAN=1
    fi
# Skip the default mvn command because we want to set the profile explicitly
install: true
script:
  - cp .travis.settings.xml $HOME/.m2/settings.xml
  - mvn -e -Pdistribution-as-parent-pom test
  # Deploy Maven artifacts, but only from regular master builds
  - |
    if [ "$TRAVIS_BRANCH" == "master" -a "$TRAVIS_PULL_REQUEST" == "false" ]
    then
      mvn -e -Pdistribution-as-parent-pom -DskipTests deploy
    fi
